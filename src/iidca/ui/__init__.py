"""Dashboard UI helpers — theme, readings, charts, components.

Everything here is presentation-only: it renders states produced by the
engines and never contains business logic or thresholds of its own
(thresholds come from config / engine outputs).
"""
